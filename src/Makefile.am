## Process this file with automake to produce Makefile.in

## Subdirectories

if SYSTEMC_SUPPORT
  SC_DEP_DIRS = aclib acsim accsim
else
  SC_DEP_DIRS =
endif
SUBDIRS = acpp $(SC_DEP_DIRS) acbinutils
